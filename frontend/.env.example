# Backend API origin.
# Local dev: leave unset — Vite proxy handles /api → http://localhost:8000
# Production: set to your deployed backend URL (no trailing slash)
# VITE_API_BASE_URL=https://procurement-agent-api.onrender.com
